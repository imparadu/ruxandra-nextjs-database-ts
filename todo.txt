tutorial storage
admin dashboard layout:
   | login fields with login button
   - > left side nav buttons: [portfolio, sketchbook, shop]
dashboard:
1. add pic
 upload product: {
  id: number;
  title: string;
  price: number;
  currency: string;
  imgUrl: string;
  width: number;
  height: number;
  alt: string;
}
select pic
add button
2. edit
3. remove

env for auth

css admin, dashboard
